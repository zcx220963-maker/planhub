"""
Parameter Extractor 节点提示词（工具选择与参数提取）
"""

TOOL_SELECTOR_SYSTEM_PROMPT = "你是一个工具选择器，只输出 JSON，不输出其他内容。"

TOOL_SELECTOR_PROMPT_TEMPLATE = """你是一个工具选择器。根据计划信息，从候选工具中选择需要调用的工具并提取参数。

【计划信息】
{plan_summary}

【候选工具】
{tools_desc}

请完成两件事：
a. 对每个工具打分 0-10（≥6 分才选，低于6分不选）
b. 从计划信息中按参数要求提取参数值（禁止编造、禁止推测）

输出格式（JSON）：
{{"rankings": [
  {{"tool": "tool_name", "score": 分数, "params": {{"slot名": "值"}}}}
]}}

注意：
- 只输出 JSON
- 没有明确信息的参数不要填
- 同一个工具可以根据需要调用多次（比如多个城市分别调用天气工具）
- 但要区分：只有目的地/游玩地点才调用城市介绍和天气工具，出发地、途经地不需要调用
- 城市名、地点名可以从计划信息中提取
"""
